<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Library Management - Add Book</title>
    <style>
        /* General body styling */
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f9f9f9;
            margin: 0;
            padding: 0;
        }

        /* Header styling */
        header {
            width: 100%;
            background-color: #2c3e50;
            color: white;
            padding: 20px 0;
            text-align: center;
            position: fixed;
            top: 0;
            z-index: 100;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        header h1 {
            margin: 0;
            font-size: 32px;
            font-weight: bold;
            letter-spacing: 1.5px;
        }

        header nav {
            margin-top: 10px;
        }

        header nav a {
            color: white;
            text-decoration: none;
            margin: 0 20px;
            font-size: 18px;
            font-weight: bold;
        }

        header nav a:hover {
            color: #3498db;
        }

        /* Hero section styling */
        .hero {
            margin-top: 120px;
            background-color: #ecf0f1;
            padding: 50px 20px;
            text-align: center;
        }

        .hero h2 {
            font-size: 36px;
            color: #2c3e50;
        }

        .hero p {
            font-size: 18px;
            color: #7f8c8d;
            max-width: 600px;
            margin: 20px auto;
        }

        .hero a {
            display: inline-block;
            padding: 10px 20px;
            background-color: #3498db;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            font-size: 18px;
            transition: background-color 0.3s ease;
        }

        .hero a:hover {
            background-color: #2980b9;
        }

        /* Main container styling */
        .container {
            margin: 40px auto;
            background-color: white;
            width: 60%;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        h2 {
            color: #333;
            margin-bottom: 20px;
            text-align: center;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        td {
            padding: 10px;
            text-align: left;
        }

        input[type="text"],
        input[type="number"],
        select {
            width: 100%;
            padding: 10px;
            margin: 5px 0;
            border: 1px solid #ccc;
            border-radius: 5px;
        }

        input[type="submit"] {
            background-color: #27ae60;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
        }

        input[type="submit"]:hover {
            background-color: #2ecc71;
        }

        /* Book categories section */
        .categories {
            margin: 50px auto;
            text-align: center;
        }

        .categories h3 {
            color: #333;
            margin-bottom: 20px;
            font-size: 24px;
        }

        .categories .category-list {
            display: flex;
            justify-content: center;
            gap: 20px;
            flex-wrap: wrap;
        }

        .category {
            padding: 20px;
            background-color: #3498db;
            color: white;
            border-radius: 10px;
            transition: transform 0.3s ease;
            font-size: 18px;
        }

        .category:hover {
            transform: scale(1.05);
            background-color: #2e3b44;
        }

        /* Footer styling */
        footer {
            background-color: #2c3e50;
            color: white;
            padding: 20px 0;
            text-align: center;
            margin-top: 40px;
        }

        footer p {
            margin: 0;
            font-size: 14px;
        }

        /* Responsive design */
        @media (max-width: 768px) {
            .container {
                width: 80%;
            }

            .categories .category-list {
                flex-direction: column;
            }
        }

        @media (max-width: 480px) {
            header h1 {
                font-size: 24px;
            }

            .container {
                width: 90%;
            }
        }
    </style>
</head>

<body>

    <!-- Header Section -->
    <header>
        <h1>Library Management</h1>
        <nav>
            <a href="/">Home</a>
            <a href="/view">View Books</a>
            <a href="/add">Add Book</a>
        </nav>
    </header>

    <!-- Hero Section -->
    <section class="hero">
        <h2>Welcome to Our Library</h2>
        <p>Explore our vast collection of books, discover new authors, and expand your knowledge with our wide range of categories.</p>
        <a href="#">Explore Books</a>
    </section>

    <!-- Main Container for Adding a Book -->
    <div class="container">
        <h2>Add a New Book</h2>
        <form action="/insertRecord" method="post">
            <table>
                <tr>
                    <td>Book Name:</td>
                    <td><input type="text" name="book_name" required></td>
                </tr>
                <tr>
                    <td>Book Price:</td>
                    <td><input type="number" name="book_price" required></td>
                </tr>
                <tr>
                    <td>Book Pages:</td>
                    <td>
                        <select name="book_pages" required>
                            <option>--- Select Pages Range ---</option>
                            <% 
                            const page = ["200 to 300", "300 to 400", "400 to 500", "500 to 600"];
                            page.map((p) => { %>
                                <option value="<%= p %>"><%= p %></option>
                            <% }) %>
                        </select>
                    </td>
                </tr>
                <tr>
                    <td>Book Author:</td>
                    <td><input type="text" name="book_author" required></td>
                </tr>
                <tr>
                    <td></td>
                    <td><input type="submit" value="Add Book"></td>
                </tr>
            </table>
        </form>
    </div>

    <!-- Book Categories Section -->
    <section class="categories">
        <h3>Browse by Category</h3>
        <div class="category-list">
            <div class="category">Fiction</div>
            <div class="category">Non-Fiction</div>
            <div class="category">Science</div>
            <div class="category">History</div>
            <div class="category">Biography</div>
        </div>
    </section>

    <!-- Footer Section -->
    <footer>
        <p>&copy; 2024 Library Management. All Rights Reserved.</p>
    </footer>

</body>

</html>
